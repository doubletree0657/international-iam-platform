spring:
  application:
    name: international-iam-platform
  datasource:
    url: jdbc:postgresql://localhost:5432/international_iam
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: none

iam:
  security:
    # Local development only. Do not use this key in production; use secure external key management.
    secret-encryption-key: MDEyMzQ1Njc4OWFiY2RlZjAxMjM0NTY3ODlhYmNkZWY=
